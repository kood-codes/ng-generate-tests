import { TestBed } from '@angular/core/testing';
import { <%-name%> } from './<%-fileName%>';

describe('<%-name%>', () => {
  let pipe: <%-name%>;
  beforeEach(() => {
    TestBed.configureTestingModule({ providers: [<%-name%>] });
    pipe = TestBed.inject(<%-name%>);
  });

  it('can load instance', () => {
    expect(pipe).toBeTruthy();
  });

  it('transforms X to Y', () => {
    expect(pipe.transform('X')).toEqual('Y');
  });
});